import { HighelatesDirective } from './highelates.directive';

describe('HighelatesDirective', () => {
  it('should create an instance', () => {
    const directive = new HighelatesDirective();
    expect(directive).toBeTruthy();
  });
});
